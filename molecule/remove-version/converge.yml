---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  roles:
  - role: hth.deno
    vars:
      deno:
        primary: "2.8.2"
        versions:
          - version: "2.8.1"
            remove: true
